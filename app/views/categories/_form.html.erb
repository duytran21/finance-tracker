<%= form_for(category) do |f| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

    <div class="col-sm-2"><%= f.label :name , :class => 'field-label-cust' %></div>
    <div class="col-sm-10"><%= f.text_field :name , placeholder: "Enter Category", :class => 'field-box-cust text-field-cust' %></div>
  </div>

  <div class="actions">
    <%= f.submit :class => 'btn btn-black btn-primary' %>
  </div>
<% end %>
